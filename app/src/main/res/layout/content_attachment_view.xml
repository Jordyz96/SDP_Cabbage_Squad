<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.cabbage.sdpjournal.AttachmentViewActivity"
    tools:showIn="@layout/activity_attachment_view">

    <TextView
        android:layout_width="368dp"
        android:layout_height="wrap_content"
        android:text="Image Attachment"
        android:textSize="30dp"
        android:textStyle="italic"
        android:layout_centerHorizontal="true"
        android:id="@+id/tvImageAttachmentLabel"
        android:gravity="center_horizontal"/>
    <GridView
        android:layout_width="368dp"
        android:layout_height="300dp"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="8dp"
        android:id="@+id/gvImageView"
        android:columnWidth="150dp"
        android:numColumns="auto_fit"
        android:layout_below="@+id/tvImageAttachmentLabel"
        />

    <TextView
        android:layout_width="368dp"
        android:layout_height="wrap_content"
        android:text="Audio Attachment"
        android:textStyle="italic"
        android:layout_marginTop="8dp"
        android:textSize="30dp"
        android:layout_below="@+id/gvImageView"
        android:id="@+id/tvAudioAttachmentLabel"
        android:gravity="center_horizontal"
        android:layout_centerHorizontal="true"/>

    <ListView
        android:layout_width="368dp"
        android:layout_height="100dp"
        android:layout_below="@+id/tvAudioAttachmentLabel"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="8dp"
        android:id="@+id/lvAudioListView"
        />

</RelativeLayout>
